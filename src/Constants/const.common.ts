/**
 * @description in this class are stored all the  values and the files names/prefixes
 * - this is a class and not an enum to support also non strings/numeric values.
 * -- sparse in the library there are  '// static' comments indicating static strings that are not placed
 * in the constant.files just becouse i didn't know how to organize them
 */
export class ConstCommon {
    static prefix = "WTM";
}